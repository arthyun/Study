let initialState = {
    user : {
        id: "heun3316",
        pass: "hyun7212"
    }
}
const Login = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
};

export default Login;